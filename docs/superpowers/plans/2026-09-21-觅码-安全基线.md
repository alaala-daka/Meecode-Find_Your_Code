# 觅码 · 安全基线 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按《2026-09-21-觅码-安全基线-design.md》落地四层基线 + 一个顺手项：`backend/app/security.py` 内存滑动窗口限流与 Origin 白名单校验（零新依赖）、nginx 安全响应头与 `/explain-api/` XFF 修复、CI test 门禁、`index.html` 基础分享 meta、runbook 安全章节。全部完成后合回 `feat/frontend-ui` 触发部署，nginx 部分按 runbook 手动 reload。

**Architecture:** 安全横切关注点收敛在新增的 `backend/app/security.py`（限流器 + Origin 校验 + IP/键提取三个纯函数 + 一个 `SecurityMiddleware`），配置集中在 `config.py` 安全段（环境变量可调）；中间件经 `main.py` 挂载，层序为「security 先加（内层）→ CORS 后加（外层）」保证 429 带 CORS 头；nginx 侧加 6 个安全响应头 + `server_tokens off` + `client_max_body_size 1m` + explain-api 的 XFF；CI 拆出独立 `test` job 作 `deploy` 的前置门禁。

**Tech Stack:** Python stdlib（time）+ Starlette `BaseHTTPMiddleware` + FastAPI 既有骨架；GitHub Actions（ubuntu-latest, Python 3.12, Node 20）；Nginx add_header；无任何新第三方依赖。

## Global Constraints

- **零新依赖**：`security.py` 只用 stdlib 与 starlette/fastapi 已有能力；`requirements.txt` 与 `package.json` 本计划一律不动。
- **中间件层序红线**：`main.py` 中 `app.add_middleware(SecurityMiddleware)` 必须写在 `CORSMiddleware` **之前**（Starlette 头插建栈，后加者包外层）。顺序反了 429 将不带 CORS 头，浏览器侧报 CORS 错误而非 429。
- **请求时求值**：中间件必须经 `config.RATE_LIMIT_ENABLED` / `config.RATE_LIMITS` 属性访问读取配置（不得在模块层绑定常量），保证测试 `monkeypatch.setattr` 生效。
- **不碰现有语义**：任何路由函数、响应结构、DTO 契约零改动；存量 196 个后端 + 205 个前端测试必须全绿。
- **工作区有无关未提交改动**（`backend/meecode.db-wal/shm`、`新一轮完善.txt`、spec 文件等）：每个提交步骤只 `git add` 本任务明确列出的文件，禁止 `git add -A` / `git add .`。
- 分支纪律：从 `feat/frontend-ui` 切 `feat/security-baseline` 开发（该分支不触发部署）；CI 全绿后合回 `feat/frontend-ui` 才上线后端/前端部分。
- nginx 模板中 `<你的域名>` 是部署参数占位，保持原样；`proxy_buffering off` 与 `proxy_read_timeout 300s`（解读域流式依赖）不得删。
- CI 的 pytest 步骤必须显式注入 `LLM_MOCK=true GITHUB_MOCK=true`（CI 无 `.env`，config 默认 mock=false 且 `ensure_prod_secrets` 会拒启）。

---

### Task 1: 切分支 + config.py 安全参数

**Files:**
- Modify: `backend/app/config.py`（会话段之后新增安全段）
- Test: 无（纯配置，Task 3 的行为测试覆盖）

**Interfaces:**
- Consumes: 既有 `CORS_ORIGINS`（config.py:36）、`SESSION_COOKIE`（config.py:32）。
- Produces: `RATE_LIMIT_ENABLED` / `RATE_LIMIT_WINDOW` / `RATE_LIMIT_MAX_KEYS` / `RATE_LIMITS`（dict） / `ALLOWED_ORIGINS`（tuple）——Task 3 的 `security.py` 与 conftest 消费。

- [ ] **Step 1: 切分支**

```powershell
git checkout feat/frontend-ui
git checkout -b feat/security-baseline
```

- [ ] **Step 2: 写失败验证（导入即失败）**

```powershell
# workdir: backend
python -c "from app import config; print(config.RATE_LIMIT_ENABLED)"
```

预期：`AttributeError: module 'app.config' has no attribute 'RATE_LIMIT_ENABLED'`。

- [ ] **Step 3: 最小实现**

`backend/app/config.py` 的 CORS 段（`CORS_ORIGINS` 两行）之后、`ensure_prod_secrets` 之前，插入：

```python

# ---------- 安全（限流 + Origin 校验，spec 2026-09-21） ----------
RATE_LIMIT_ENABLED: bool = os.getenv("RATE_LIMIT_ENABLED", "true").lower() in ("1", "true", "yes")
RATE_LIMIT_WINDOW: int = int(os.getenv("RATE_LIMIT_WINDOW", "60"))          # 窗口秒数
RATE_LIMIT_MAX_KEYS: int = int(os.getenv("RATE_LIMIT_MAX_KEYS", "10000"))   # 限流器键数上限
# CSRF 白名单与 CORS 同源：可信来源只维护一份
ALLOWED_ORIGINS: tuple[str, ...] = CORS_ORIGINS

_RATE_LIMIT_DEFAULTS: dict[str, int] = {
    "llm": 20,       # LLM 计费端点：ai-draft + 解读域四端点
    "session": 30,   # 白拿 session_id 是匿名打 LLM 的前置步
    "submit": 5,     # 投稿：GitHub 拉取 + LLM 精筛
    "interact": 60,  # 幂等互动写
    "browse": 240,   # feed/search/详情：匿名写库放大面
    "auth": 10,      # OAuth 跳转与回调
    "delist": 5,     # 下架：高危写操作，与 submit 同档
    "default": 120,  # 兜底
}


def _int_env(name: str, default: int) -> int:
    raw = os.getenv(name)
    try:
        return int(raw) if raw else default
    except ValueError:
        return default


# 每桶每 RATE_LIMIT_WINDOW 秒上限；环境变量 RATE_LIMIT_<桶名大写> 逐桶覆盖
RATE_LIMITS: dict[str, int] = {
    bucket: _int_env(f"RATE_LIMIT_{bucket.upper()}", limit)
    for bucket, limit in _RATE_LIMIT_DEFAULTS.items()
}
```

要点核对：`ALLOWED_ORIGINS` 直接派生自 `CORS_ORIGINS`（同文件上方已定义）；`RATE_LIMITS` 推导式保证 `default` 键必存（中间件兜底 `.get(bucket, RATE_LIMITS["default"])` 双保险）。

- [ ] **Step 4: 验证导入**

```powershell
# workdir: backend
python -c "from app import config; print(config.RATE_LIMIT_ENABLED, config.RATE_LIMITS['llm'], config.ALLOWED_ORIGINS)"
```

预期：`True 20 ('http://localhost:5173', 'http://127.0.0.1:5173')`（首项随 `.env` 的 `FRONTEND_ORIGIN` 变化）。

- [ ] **Step 5: 提交**

```powershell
git add backend/app/config.py
git commit -m "feat(security): config 安全段——限流分桶参数与 Origin 白名单（安全基线 1/8）"
```

---

### Task 2: conftest autouse 关闭限流（存量免疫）

**Files:**
- Modify: `backend/tests/conftest.py`

**Interfaces:**
- Consumes: `config.RATE_LIMIT_ENABLED`（Task 1）、`security._limiter`（Task 3 的模块级单例，本任务先写 import，Task 3 落地前本文件处于「引用未实现模块」状态——两任务必须连续执行，或本任务在 Task 3 之后提交）。
- Produces: 全部存量用例在限流关闭状态下运行；`test_security.py`（Task 3）经自身 autouse 开回（已验证：模块级 autouse 后于 conftest 执行，后者胜出顺序不颠倒）。

- [ ] **Step 1: 修改 conftest（与 Task 3 同批提交）**

`backend/tests/conftest.py` 顶部导入改为：

```python
"""测试公共装置:解读域清 LLM 客户端缓存;信息流域每用例内存库 + 清 GitHub 缓存。"""
import pytest

from app import config, llm
```

文件末尾追加：

```python
@pytest.fixture(autouse=True)
def _rate_limit_off(monkeypatch):
    """安全基线：限流默认关闭，存量用例不受分桶影响；test_security 内 autouse 自行开回。"""
    from app import security

    monkeypatch.setattr(config, "RATE_LIMIT_ENABLED", False)
    security._limiter.reset()
    yield
    security._limiter.reset()
```

- [ ] **Step 2: 提交（与 Task 3 的实现合并为一次提交，见 Task 3 Step 5）**

---

### Task 3: security.py 限流 + Origin 校验（TDD 核心）

**Files:**
- Create: `backend/app/security.py`
- Test: `backend/tests/test_security.py`（新建）
- Modify: `backend/tests/conftest.py`（Task 2 的改动随本任务一并提交）

**Interfaces:**
- Consumes: `config.RATE_LIMIT_ENABLED/WINDOW/MAX_KEYS/LIMITS/ALLOWED_ORIGINS/SESSION_COOKIE`（Task 1）；`auth.verify()`（feed/auth.py:32，纯 HMAC 计算不查库）。
- Produces:
  - `SlidingWindowLimiter`（类，`allow(key, limit, window, now, max_keys) -> tuple[bool, int]`、`reset()`）
  - `bucket_for(method, path) -> str`、`client_ip(request) -> str`、`rate_key(request) -> str`
  - `SecurityMiddleware`（BaseHTTPMiddleware 子类） + 模块级 `_limiter` 单例
  - `main.py`（Task 4）挂载消费；`test_security.py` 覆盖全部公开面。

- [ ] **Step 1: 写失败测试**

新建 `backend/tests/test_security.py`（原样写入）：

```python
"""安全中间件：限流分桶、Origin 校验、IP/键提取、CORS 层序回归。

conftest 已 autouse 关闭限流；本文件 autouse 开回（模块级 autouse 后于 conftest
执行，已实验验证），并用 monkeypatch 把阈值压到 1~2 做超限断言，避免真发几百个请求。
"""
import pytest
from fastapi.testclient import TestClient
from starlette.requests import Request

from app import config, security
from app.feed import auth, deps
from app.main import app

NOW = 1_700_000_000


@pytest.fixture(autouse=True)
def _rate_limit_on(monkeypatch):
    monkeypatch.setattr(config, "RATE_LIMIT_ENABLED", True)
    security._limiter.reset()
    yield
    security._limiter.reset()


@pytest.fixture()
def client(conn, monkeypatch):
    monkeypatch.setattr(config, "GITHUB_MOCK", True)
    app.dependency_overrides[deps.get_conn] = lambda: conn
    yield TestClient(app)
    app.dependency_overrides.clear()


def _request(headers=None, cookies=None, client_host="9.9.9.9"):
    """构造最小 HTTP 请求（不走网络），供 client_ip / rate_key 单测。"""
    raw = [(k.lower().encode(), v.encode()) for k, v in (headers or {}).items()]
    if cookies:
        raw.append((b"cookie", "; ".join(f"{k}={v}" for k, v in cookies.items()).encode()))
    scope = {"type": "http", "method": "GET", "path": "/api/me",
             "headers": raw, "client": (client_host, 12345)}
    return Request(scope)


# ---------- 纯函数 ----------

def test_client_ip_takes_last_xff_segment():
    # nginx $proxy_add_x_forwarded_for 末段由受信代理追加；前置段可伪造，弃用
    assert security.client_ip(_request(headers={"x-forwarded-for": "1.1.1.1, 2.2.2.2"})) == "2.2.2.2"


def test_client_ip_falls_back_to_socket_peer():
    assert security.client_ip(_request()) == "9.9.9.9"


def test_rate_key_prefers_logged_in_user(conn):
    uid = auth.upsert_user(conn, {"id": 42, "login": "k", "avatar_url": ""})
    req = _request(cookies={config.SESSION_COOKIE: auth.sign(uid)})
    assert security.rate_key(req) == f"u:{uid}"


def test_rate_key_falls_back_to_ip():
    assert security.rate_key(_request(headers={"x-forwarded-for": "3.3.3.3"})) == "ip:3.3.3.3"


def test_rate_key_bad_signature_is_anonymous():
    req = _request(cookies={config.SESSION_COOKIE: "forged.token"})
    assert security.rate_key(req).startswith("ip:")


def test_bucket_for_routes():
    assert security.bucket_for("POST", "/api/ai-draft") == "llm"
    assert security.bucket_for("POST", "/api/repos/root") == "llm"      # 先于 delist 命中
    assert security.bucket_for("POST", "/api/expand") == "llm"
    assert security.bucket_for("POST", "/api/nodes/detail") == "llm"
    assert security.bucket_for("POST", "/api/reader/chat") == "llm"
    assert security.bucket_for("POST", "/api/sessions") == "session"
    assert security.bucket_for("POST", "/api/submit") == "submit"
    assert security.bucket_for("POST", "/api/interactions") == "interact"
    assert security.bucket_for("POST", "/api/repos/7/delist") == "delist"
    assert security.bucket_for("GET", "/api/feed") == "browse"
    assert security.bucket_for("GET", "/api/repos/7") == "browse"
    assert security.bucket_for("GET", "/api/auth/callback") == "auth"
    assert security.bucket_for("GET", "/api/me") == "default"


# ---------- 限流器 ----------

def test_limiter_allows_until_limit_then_rejects():
    limiter = security.SlidingWindowLimiter()
    for i in range(3):
        assert limiter.allow("k", 3, 60, NOW + i)[0]
    allowed, retry = limiter.allow("k", 3, 60, NOW + 4)
    assert not allowed and retry >= 1


def test_limiter_window_slide_recovers():
    limiter = security.SlidingWindowLimiter()
    limiter.allow("k", 2, 60, NOW)
    limiter.allow("k", 2, 60, NOW + 1)
    assert not limiter.allow("k", 2, 60, NOW + 1)[0]
    assert limiter.allow("k", 2, 60, NOW + 61)[0]       # 窗口滑过即恢复


def test_limiter_evicts_oldest_key_at_capacity():
    limiter = security.SlidingWindowLimiter()
    for i in range(3):
        limiter.allow(f"k{i}", 5, 60, NOW)
    limiter.allow("new", 5, 60, NOW, max_keys=3)
    assert len(limiter._hits) == 3                       # 淘汰最早加入的 k0
    assert "new" in limiter._hits and "k0" not in limiter._hits


# ---------- 中间件（经 TestClient 走真实栈） ----------

def test_llm_endpoint_429_with_retry_after(client, monkeypatch):
    monkeypatch.setattr(config, "RATE_LIMITS", {**config.RATE_LIMITS, "session": 2})
    assert client.post("/api/sessions").status_code == 200
    assert client.post("/api/sessions").status_code == 200
    resp = client.post("/api/sessions")
    assert resp.status_code == 429
    assert int(resp.headers["retry-after"]) >= 1
    assert "detail" in resp.json()


def test_429_still_carries_cors_header(client, monkeypatch):
    # 层序回归：security 内层、CORS 外层，429 出栈时补 CORS 头
    monkeypatch.setattr(config, "RATE_LIMITS", {**config.RATE_LIMITS, "default": 1})
    origin = {"Origin": "http://localhost:5173"}
    client.get("/api/me", headers=origin)
    resp = client.get("/api/me", headers=origin)
    assert resp.status_code == 429
    assert resp.headers["access-control-allow-origin"] == "http://localhost:5173"


def test_origin_not_in_whitelist_rejected(client):
    resp = client.put("/api/me/bio", json={"bio": "x"},
                      headers={"Origin": "https://evil.example"})
    assert resp.status_code == 403


def test_missing_origin_passes_middleware(client):
    # 无 Origin（curl/TestClient）：放行至路由层，由路由自己的 401 回答
    assert client.put("/api/me/bio", json={"bio": "x"}).status_code == 401


def test_allowed_origin_passes_middleware(client):
    resp = client.put("/api/me/bio", json={"bio": "x"},
                      headers={"Origin": "http://localhost:5173"})
    assert resp.status_code == 401


def test_get_with_bad_origin_not_blocked(client):
    # GET 非状态变更，不在 CSRF 威胁模型内
    assert client.get("/api/me", headers={"Origin": "https://evil.example"}).json() is None


def test_rate_limit_disabled_passes(client, monkeypatch):
    monkeypatch.setattr(config, "RATE_LIMIT_ENABLED", False)
    monkeypatch.setattr(config, "RATE_LIMITS", {**config.RATE_LIMITS, "default": 1})
    for _ in range(4):
        assert client.get("/api/me").status_code == 200
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
# workdir: backend
python -m pytest tests/test_security.py -q
```

预期：FAIL——`ModuleNotFoundError: No module named 'app.security'`（Task 2 的 conftest 改动此时也会因 import 失败报错，属预期）。

- [ ] **Step 3: 最小实现**

新建 `backend/app/security.py`（原样写入）：

```python
"""安全中间件：内存滑动窗口限流 + Origin 白名单 CSRF 校验。

零第三方依赖：计数器为进程内 dict，单 worker 部署（systemd 单元）语义完备。
限流键登录用户优先（HMAC cookie 验签，不查库），匿名回退 XFF 末段 IP
（nginx $proxy_add_x_forwarded_for 由受信代理追加，前置段可伪造，弃用）。
设计依据：docs/superpowers/specs/2026-09-21-觅码-安全基线-design.md。
"""
from __future__ import annotations

import time

from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import JSONResponse

from . import config
from .feed import auth

API_PREFIX = "/api"
SAFE_METHODS = {"GET", "HEAD", "OPTIONS", "TRACE"}

# (方法或 None=不限, 路径前缀, 桶名)；按序首个命中，未命中落 default。
# llm 五条必须排在 delist 之前：POST /api/repos/root 同时匹配 /api/repos/ 前缀。
_RATE_RULES: tuple[tuple[str | None, str, str], ...] = (
    ("POST", "/api/ai-draft", "llm"),
    ("POST", "/api/repos/root", "llm"),
    ("POST", "/api/expand", "llm"),
    ("POST", "/api/nodes/detail", "llm"),
    ("POST", "/api/reader/chat", "llm"),
    ("POST", "/api/sessions", "session"),
    ("POST", "/api/submit", "submit"),
    ("POST", "/api/interactions", "interact"),
    ("POST", "/api/repos/", "delist"),
    ("GET", "/api/feed", "browse"),
    ("GET", "/api/search", "browse"),
    ("GET", "/api/repos/", "browse"),
    (None, "/api/auth/", "auth"),
)


def bucket_for(method: str, path: str) -> str:
    for rule_method, prefix, bucket in _RATE_RULES:
        if path.startswith(prefix) and (rule_method is None or rule_method == method):
            return bucket
    return "default"


def client_ip(request: Request) -> str:
    """取 XFF 末段（nginx 受信追加），无 XFF 回退 socket 对端。"""
    forwarded = request.headers.get("x-forwarded-for", "")
    if forwarded:
        return forwarded.split(",")[-1].strip()
    return request.client.host if request.client else ""


def rate_key(request: Request) -> str:
    """登录用户按 user_id（验签纯计算不查库），匿名按 IP。"""
    token = request.cookies.get(config.SESSION_COOKIE, "")
    user_id = auth.verify(token) if token else None
    return f"u:{user_id}" if user_id else f"ip:{client_ip(request)}"


class SlidingWindowLimiter:
    """进程内滑动窗口计数。多 worker 部署时各进程独立计数（见 spec 风险表）。"""

    def __init__(self) -> None:
        self._hits: dict[str, list[float]] = {}

    def allow(self, key: str, limit: int, window: float, now: float,
              max_keys: int = 10_000) -> tuple[bool, int]:
        """第 2 个返回值为超限时的 Retry-After 秒数（≥1）。"""
        hits = self._hits.get(key)
        if hits is None:
            if len(self._hits) >= max_keys:
                # 键数上限：淘汰最早加入的键（dict 保序），防内存无限增长
                self._hits.pop(next(iter(self._hits)), None)
            hits = []
        hits = [t for t in hits if t > now - window]     # 剪枝过期命中即清理
        if len(hits) >= limit:
            self._hits[key] = hits
            return False, max(1, int(hits[0] + window - now))
        hits.append(now)
        self._hits[key] = hits
        return True, 0

    def reset(self) -> None:
        self._hits.clear()


_limiter = SlidingWindowLimiter()


class SecurityMiddleware(BaseHTTPMiddleware):
    """先 Origin 校验（403），再限流（429）；仅拦 /api 前缀路径。"""

    async def dispatch(self, request: Request, call_next):
        path = request.url.path
        if not path.startswith(API_PREFIX):
            return await call_next(request)

        if request.method not in SAFE_METHODS:
            origin = request.headers.get("origin")
            if origin and origin not in config.ALLOWED_ORIGINS:
                return JSONResponse({"detail": "非法来源请求"}, status_code=403)

        if config.RATE_LIMIT_ENABLED:
            limit = config.RATE_LIMITS.get(
                bucket_for(request.method, path), config.RATE_LIMITS["default"]
            )
            allowed, retry_after = _limiter.allow(
                rate_key(request), limit, config.RATE_LIMIT_WINDOW,
                time.time(), config.RATE_LIMIT_MAX_KEYS,
            )
            if not allowed:
                return JSONResponse(
                    {"detail": "请求过于频繁，请稍后再试"},
                    status_code=429,
                    headers={"Retry-After": str(retry_after)},
                )
        return await call_next(request)
```

要点核对：`_RATE_RULES` 中 llm 五条先于 `("POST", "/api/repos/", "delist")`（`/api/repos/root` 前缀冲突）；`RATE_LIMITS["default"]` 兜底依赖 Task 1 推导式必产 `default` 键；`auth.verify` 异常内部已吞（返回 None），伪造 cookie 落匿名键。

- [ ] **Step 4: 跑绿 + 全量回归**

```powershell
# workdir: backend
python -m pytest tests/test_security.py -q
python -m pytest -q
```

预期：`test_security.py` 全绿（16 个用例）；全量约 212 个用例全绿（196 存量 + 16 新增），conftest 关闭限流后存量零行为变化。

- [ ] **Step 5: 提交（含 Task 2 的 conftest 改动）**

```powershell
git add backend/app/security.py backend/tests/test_security.py backend/tests/conftest.py
git commit -m "feat(security): 内存滑动窗口限流 + Origin 白名单校验，零新依赖（安全基线 2/8）"
```

---

### Task 4: main.py 挂载中间件（层序）

**Files:**
- Modify: `backend/app/main.py`（导入 + CORS 段）

**Interfaces:**
- Consumes: `SecurityMiddleware`（Task 3）。
- Produces: 应用中间件栈「CORS 外层 / security 内层」；Task 3 的 `test_429_still_carries_cors_header` 即本任务的层序回归。

- [ ] **Step 1: 写失败验证（层序断言先红后绿法不适用——本任务由 Task 3 已有用例守护，直接实现）**

直接实现，理由：`test_security.py::test_429_still_carries_cors_header` 在 Task 3 已断言「429 带 CORS 头」——层序写反时该用例必然红，无需新测试。

- [ ] **Step 2: 最小实现**

`backend/app/main.py` 顶部导入区，在 `from .feed import db` 之后加一行：

```python
from .security import SecurityMiddleware
```

把现有 CORS 段：

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=list(config.CORS_ORIGINS),
    allow_credentials=True,  # 登录态是 cookie,必须允许
    allow_methods=["*"],
    allow_headers=["*"],
)
```

替换为：

```python
# 层序红线：security 先加（内层），CORS 后加（外层）——429 出栈时经 CORS 补头
app.add_middleware(SecurityMiddleware)
app.add_middleware(
    CORSMiddleware,
    allow_origins=list(config.CORS_ORIGINS),
    allow_credentials=True,  # 登录态是 cookie,必须允许
    allow_methods=["*"],
    allow_headers=["*"],
)
```

- [ ] **Step 3: 验证**

```powershell
# workdir: backend
python -m pytest -q
```

预期：全绿。重点确认 `test_security.py::test_429_still_carries_cors_header` 通过（层序正确）；若把两行 add_middleware 顺序对调，该用例应变红（自检时可选验证）。

- [ ] **Step 4: 提交**

```powershell
git add backend/app/main.py
git commit -m "feat(security): main.py 挂载安全中间件，层序 security 内 CORS 外（安全基线 3/8）"
```

---

### Task 5: CI test 门禁

**Files:**
- Modify: `.github/workflows/deploy.yml`（新增 test job + deploy needs）

**Interfaces:**
- Consumes: 既有 deploy job 全步骤（原样保留）；仓库 Secrets（不变）。
- Produces: `test` job（backend pytest + frontend typecheck + vitest）作为 `deploy` 的前置；push 到 `feat/frontend-ui` 时测试红即不部署。

- [ ] **Step 1: 最小实现**

`.github/workflows/deploy.yml` 全文替换为：

```yaml
name: deploy

on:
  push:
    branches: [feat/frontend-ui]
    paths:
      - 'frontend/**'
      - 'backend/**'
      - '.github/workflows/deploy.yml'

concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Backend tests (mock mode, offline)
        working-directory: backend
        env:
          LLM_MOCK: 'true'
          GITHUB_MOCK: 'true'
        run: |
          pip install -r requirements.txt
          pytest -q

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Frontend typecheck
        working-directory: frontend
        run: npm ci && npm run typecheck

      - name: Frontend tests
        working-directory: frontend
        run: npm test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        env:
          VITE_USE_MOCK: 'false'
        run: npm ci && npm run build

      - name: Setup SSH key
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" | sed 's/\r$//' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keygen -y -f ~/.ssh/deploy_key > /dev/null
          echo "Secret 私钥指纹: $(ssh-keygen -y -f ~/.ssh/deploy_key | ssh-keygen -lf -)"
          ssh-keyscan -H "${SSH_HOST##*@}" >> ~/.ssh/known_hosts

      - name: Upload frontend dist
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          rsync -az --delete -e "ssh -i ~/.ssh/deploy_key" frontend/dist/ "$SSH_HOST:/var/www/meecode/"

      - name: Deploy backend
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          ssh -i ~/.ssh/deploy_key "$SSH_HOST" '
            set -e
            cd /opt/meecode
            git pull --ff-only
            backend/.venv/bin/pip install -r backend/requirements.txt
            sudo systemctl restart meecode-backend
          '

      - name: Health check
        env:
          DEPLOY_DOMAIN: ${{ secrets.DEPLOY_DOMAIN }}
        run: |
          for i in 1 2 3 4 5; do
            if curl -fsS "https://${DEPLOY_DOMAIN}/explain-api/health"; then exit 0; fi
            sleep 5
          done
          exit 1
```

要点核对：与旧版 diff 仅两处——新增 `test` job、`deploy` 加 `needs: test`；旧 deploy 全部步骤原样保留（含私钥指纹打印与并发组）。

- [ ] **Step 2: 本地核对清单（无 Actions 运行时，人工核对）**

```text
[ ] YAML 可解析：python -c "import yaml,io;yaml.safe_load(io.open('.github/workflows/deploy.yml',encoding='utf-8'))"（PyYAML 不可用时跳过）
[ ] deploy.jobs 有且仅有 needs: test 一处新增
[ ] test job 的 pytest 步骤 env 含 LLM_MOCK=true 与 GITHUB_MOCK=true（缺则 ensure_prod_secrets 拒启）
[ ] test job 三个步骤命令与本地一致：pytest -q / npm run typecheck / npm test
[ ] concurrency 组名仍为 deploy-prod；触发分支/paths 未动
```

- [ ] **Step 3: 提交**

```powershell
git add .github/workflows/deploy.yml
git commit -m "ci: 新增 test 门禁 job（pytest+typecheck+vitest），deploy needs test（安全基线 4/8）"
```

---

### Task 6: nginx 安全响应头 + explain-api XFF 修复

**Files:**
- Modify: `deploy/nginx-meecode.conf`

**Interfaces:**
- Consumes: 现网既有结构（80 跳转、443 静态、`/explain-api/` 反代、`/api/` 反代）；`proxy_buffering off` / `proxy_read_timeout 300s` 必须保留（解读域 NDJSON 流式）。
- Produces: 上线后全站响应带 6 个安全头；`/explain-api/` 带 XFF（Task 3 限流 IP 键的前置——缺它解读域流量全共享 127.0.0.1 一桶）；runbook（Task 8）第 7 步 reload 生效。

- [ ] **Step 1: 最小实现**

`deploy/nginx-meecode.conf` 全文替换为：

```nginx
# 觅码线上站点配置（最终形态）
# 使用方式见 docs/deploy-runbook.md 第 5/7 步：先装 80 端口最小配置签证书，
# 再替换为本文件并 reload。部署前把 <你的域名> 全部替换为实际域名。
server {
    listen 80;
    server_name <你的域名>;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name <你的域名>;

    ssl_certificate     /etc/letsencrypt/live/<你的域名>/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/<你的域名>/privkey.pem;

    root /var/www/meecode;
    index index.html;

    server_tokens off;
    client_max_body_size 1m;

    # 安全响应头（安全基线 2026-09-21；always = 错误响应也带头）
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    # CSP：无 CDN 外链（已 grep 确认）；img 放行 https:（README 用户内容图片）
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data: https:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self'; font-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /explain-api/ {
        proxy_pass http://127.0.0.1:8100/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # 必须补：本 location 出现任一 proxy_set_header 后，nginx 默认的
        # X-Forwarded-For 即被清空；缺此行解读域流量在后端全共享 127.0.0.1，
        # 匿名 LLM 限流退化成全局一桶（误伤 + 失效）。
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_read_timeout 300s;
    }

    # 信息流后端(与解读同进程,端口 8100;/api 无 rewrite,后端本身就挂在 /api 下)
    location /api/ {
        proxy_pass http://127.0.0.1:8100;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }
}
```

- [ ] **Step 2: 本地核对清单（无 nginx 运行时则人工核对；有 nginx 则 `nginx -t`）**

```text
[ ] 6 个 add_header 均带 always；server_tokens off 与 client_max_body_size 1m 在位
[ ] /explain-api/ 的 proxy_pass 尾斜杠未动（/explain-api/health → /api/health）
[ ] proxy_buffering off 与 proxy_read_timeout 300s 两个 location 都在
[ ] <你的域名> 占位原样保留（5 处）
[ ] 与旧版 diff：新增头/XFF/client_max_body_size/server_tokens，无删除项
```

- [ ] **Step 3: 提交**

```powershell
git add deploy/nginx-meecode.conf
git commit -m "chore(deploy): nginx 安全响应头(CSP/HSTS/nosniff)+explain-api 补 XFF（安全基线 5/8）"
```

---

### Task 7: index.html 基础分享 meta

**Files:**
- Modify: `frontend/index.html`

**Interfaces:**
- Consumes: 既有 title/description/favicon（不动）；`frontend/public/icon-512.png`（已存在，og:image 暂用）。
- Produces: 分享/抓取时的 OG 与 Twitter 卡片、首页 canonical；构建进 `dist/index.html`。

- [ ] **Step 1: 最小实现**

`frontend/index.html` 的 `<head>` 内，`<meta name="theme-color" ...>` 之后、`<title>` 之前，插入：

```html
    <link rel="canonical" href="https://www.meecode.tech/" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="觅码 Meecode" />
    <meta property="og:title" content="觅码 · 发现潜力开源仓库" />
    <meta property="og:description" content="发现潜力开源仓库，让好代码被看见。" />
    <meta property="og:url" content="https://www.meecode.tech/" />
    <meta property="og:image" content="https://www.meecode.tech/icon-512.png" />
    <meta name="twitter:card" content="summary_large_image" />
```

- [ ] **Step 2: 构建验证**

```powershell
# workdir: frontend
npm run build
Select-String -Path dist\index.html -Pattern 'og:image|canonical|twitter:card' | Measure-Object -Line
```

预期：构建成功；`dist/index.html` 命中 3 行（og:image / canonical / twitter:card 各一）。

- [ ] **Step 3: 提交**

```powershell
git add frontend/index.html
git commit -m "feat(frontend): index.html 基础分享 meta（OG/Twitter/canonical，安全基线顺手项 6/8）"
```

---

### Task 8: runbook 安全章节

**Files:**
- Modify: `docs/deploy-runbook.md`（末尾追加第 13 节）

**Interfaces:**
- Consumes: Task 5 的 CI test job、Task 6 的 nginx conf、Task 1 的限流环境变量。
- Produces: 安全基线的服务器侧手动上线步骤（nginx reload 不在 CI 内）、验证清单、限流调参、回滚。

- [ ] **Step 1: 追加章节**

`docs/deploy-runbook.md` 文末追加（原样写入）：

```markdown

## 13. 安全基线上线（2026-09-21，手动步骤）

CI 只同步 `frontend/dist/` 与后端代码，**不碰 nginx 配置**。安全头需在服务器手动生效一次：

1. 拉取含安全基线的代码后替换配置：

```bash
cd /opt/meecode && git pull --ff-only
cp /opt/meecode/deploy/nginx-meecode.conf /etc/nginx/sites-available/meecode
vi /etc/nginx/sites-available/meecode   # <你的域名> → 实际域名（若尚未替换）
nginx -t && systemctl reload nginx
```

2. 验证响应头（应逐条命中）：

```bash
curl -sI https://<你的域名>/ | grep -iE 'content-security-policy|x-frame-options|strict-transport-security|x-content-type-options|referrer-policy|permissions-policy'
```

3. 验证限流（连续 25 次打解读会话端点，第 21 次起应 429 带 Retry-After）：

```bash
for i in $(seq 1 25); do curl -s -o /dev/null -w '%{http_code}\n' -X POST https://<你的域名>/api/sessions; done | sort | uniq -c
```

4. 限流参数调整（`backend/.env` 增删后 `systemctl restart meecode-backend`）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `RATE_LIMIT_ENABLED` | `true` | 总开关 |
| `RATE_LIMIT_WINDOW` | `60` | 窗口秒数 |
| `RATE_LIMIT_LLM` / `_SESSION` / `_SUBMIT` / `_INTERACT` / `_BROWSE` / `_AUTH` / `_DELIST` / `_DEFAULT` | `20/30/5/60/240/10/5/120` | 各桶每窗口次数 |
| `RATE_LIMIT_MAX_KEYS` | `10000` | 限流器键数上限（超出淘汰最早键） |

5. 回滚：删除 nginx 中 6 个 `add_header` 行 + `nginx -t && systemctl reload nginx`；限流置 `RATE_LIMIT_ENABLED=false` 并重启后端。
```

- [ ] **Step 2: 提交**

```powershell
git add docs/deploy-runbook.md
git commit -m "docs: runbook 第 13 节——安全基线上线手动步骤/验证/调参/回滚（安全基线 7/8）"
```

---

### Task 9: 全量验证、合并与推送

**Files:**
- 无新文件（验证 + 分支合并 + 推送）

**Interfaces:**
- Consumes: Task 1–8 全部产出。
- Produces: `feat/frontend-ui` 远端更新触发 CI（test 绿 → deploy 自动上线后端/前端部分）；nginx 部分留在 Task 8 runbook 手动执行。

- [ ] **Step 1: 后端全量测试**

```powershell
# workdir: backend
python -m pytest -q
```

预期：全绿（约 212 用例：196 存量 + 16 新增）。

- [ ] **Step 2: 前端测试 + 类型检查 + 生产构建**

```powershell
# workdir: frontend
npm run typecheck
npm test
$env:VITE_USE_MOCK='false'; npm run build; Remove-Item Env:VITE_USE_MOCK
```

预期：三项全过；`dist/` 产出。

- [ ] **Step 3: 核对提交内容**

```powershell
git status --short
git log --oneline -9
```

预期：8 个提交在列（Task 1/3/4/5/6/7/8 共 7 个 + Task 3 含 Task 2 合并）；工作区只剩与安全基线无关的既有未提交改动（勿动、勿提交）。

- [ ] **Step 4: 推送开发分支，观察 CI**

```powershell
git push -u origin feat/security-baseline
```

预期：push 成功；该分支不在 workflow 触发列表，不产生部署；如需提前验证 test job，可在 Actions 页对 `feat/security-baseline` 手动 Run workflow（或临时推一个 `feat/frontend-ui` 的 PR 分支）——**注意一旦推 `feat/frontend-ui` 即触发真实部署**，须确认 nginx 手动步骤已排期。

- [ ] **Step 5: 合回部署分支**

```powershell
git checkout feat/frontend-ui
git merge --no-ff feat/security-baseline -m "merge: 安全基线上线（限流+Origin 校验+CI 门禁+nginx 安全头+分享 meta）"
git push origin feat/frontend-ui
```

预期：CI `test` 绿 → `deploy` 自动完成构建与后端重启；随后按 runbook 第 13 节手动 reload nginx 并跑 §13.2 的 curl 验证，安全基线即全量生效。

---

## Self-Review 记录

- Spec 覆盖：§3 决策 1/2/3/4 → Task 1 config + Task 3 security.py；§4.1 分桶 → Task 3 `bucket_for` 与单测；§4.2 层序 → Task 4 + `test_429_still_carries_cors_header`；§4.3 Origin → Task 3 + 4 个 Origin 用例；§4.4 nginx → Task 6 + Task 8 §13.1/13.2；§4.5 CI → Task 5；§4.6 meta → Task 7；§5 测试 → Task 3 Step 1/4 + Task 9；§6 分支交付 → Global Constraints + Task 9；§7 风险缓解 → Task 8 §13.5 回滚 + 各任务核对清单。无缺口。
- 实验验证前置：conftest 与模块级 autouse 的执行顺序（模块后执行）已用临时 pytest 实验证实，Task 3 的「conftest 关、test_security 开」设计成立。
- 用例数核对：`test_security.py` 共 16 个用例（纯函数 5 + 分桶 1 + 限流器 3 + 中间件 7），Task 9 预期 196 + 16 = 212。
- 顺序陷阱处理：Task 2 的 conftest 改动引用 `app.security`（Task 3 才创建），故两任务合并为同一次提交（Task 3 Step 5），避免中间提交 import 失败。
- 一致性：桶名集合在 Task 1 推导式、Task 3 `_RATE_RULES`、Task 8 环境变量表三处一致（llm/session/submit/interact/browse/auth/delist/default）；`<你的域名>` 占位仅在 Task 6 出现且原样保留。
